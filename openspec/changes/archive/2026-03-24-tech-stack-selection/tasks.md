# Tasks: Tech Stack Selection for Melodex

**Change**: tech-stack-selection
**Spec refs**: PSC-*, BTL-*, LNT-*, TST-*, CI-*

---

## Phase 1: Project Initialization & Dependencies

- [x] 1.1 Run `npm create vite@latest melodex -- --template react` to scaffold the base project; verify `index.html`, `src/main.jsx`, `src/App.jsx` are created. _(PSC-2, PSC-3)_
- [x] 1.2 Run `npm install react@^18 react-dom@^18 tone@^15 zustand@^5` to install production dependencies. _(PSC-3)_
- [x] 1.3 Run `npm install -D vite@^5 @vitejs/plugin-react@^4` to ensure pinned build tooling dev deps. _(PSC-3, BTL-1)_
- [x] 1.4 Run `npm install -D tailwindcss@^3 autoprefixer@^10 postcss@^8` then `npx tailwindcss init -p` to generate `tailwind.config.js` and `postcss.config.js`. _(BTL-3)_
- [x] 1.5 Run `npm install -D clsx tailwind-merge` to add shadcn/ui prerequisites. _(BTL-4)_
- [x] 1.6 Run `npx shadcn@latest init` — manually created `components.json` (style=default, baseColor=slate, cssVariables=yes, alias=@/components, utils=@/lib/utils) due to shadcn v4 interactive-only CLI. _(BTL-4)_
- [x] 1.7 Run `npx shadcn@latest add button` — manually created `src/components/ui/button.jsx` with cva + radix-ui/react-slot. _(BTL-4)_
- [x] 1.8 Run `npm install -D vitest@^1 @testing-library/react@^14 @testing-library/jest-dom@^6 jsdom@^24` to install testing deps. _(TST-1, PSC-3)_
- [x] 1.9 Run `npm install -D eslint@^8 eslint-plugin-react@latest eslint-plugin-react-hooks@latest prettier@^3` to install lint/format deps. _(LNT-1, LNT-2, PSC-3)_

---

## Phase 2: Configuration Files

- [x] 2.1 Create/overwrite `vite.config.js` with `@vitejs/plugin-react`, `resolve.alias: { '@': path.resolve(__dirname, './src') }`, and `test: { globals: true, environment: 'jsdom', setupFiles: ['./src/__tests__/setup.js'], coverage: { provider: 'v8' } }`. _(BTL-1, BTL-4)_
- [x] 2.2 Create `jsconfig.json` with `compilerOptions.paths: { "@/*": ["./src/*"] }`, `jsx: "react-jsx"`, `checkJs: false`. _(BTL-4)_
- [x] 2.3 Update `tailwind.config.js` content array to `['./index.html', './src/**/*.{js,jsx}']`. _(BTL-3)_
- [x] 2.4 Verify `postcss.config.js` includes `tailwindcss: {}` and `autoprefixer: {}` plugins (created by `tailwindcss init -p` in 1.4). _(BTL-3)_
- [x] 2.5 Create `.eslintrc.cjs` with `env: { browser: true, es2022: true }`, extends `eslint:recommended` + `plugin:react/recommended` + `plugin:react-hooks/recommended`, `settings.react.version: 'detect'`, rules: `no-unused-vars: 'warn'`, `react/react-in-jsx-scope: 'off'`, ignorePatterns: `['dist/', 'node_modules/']`. _(LNT-1)_
- [x] 2.6 Create `.prettierrc` with `printWidth: 100`, `singleQuote: true`, `semi: true`, `trailingComma: "es5"`, `tabWidth: 2`. _(LNT-2)_
- [x] 2.7 Create `.prettierignore` containing `dist/` and `node_modules/`. _(LNT-2)_
- [x] 2.8 Update `package.json` scripts to include: `"test": "vitest run"`, `"test:watch": "vitest"`, `"lint": "eslint src --ext js,jsx"`, `"format": "prettier --write src"`. _(PSC-3)_

---

## Phase 3: Source File Structure

- [x] 3.1 Update `src/index.css` to include the three Tailwind directives at the top: `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;`. _(BTL-3)_
- [x] 3.2 Update `src/main.jsx` to mount `<App />` into `<div id="root">` using `ReactDOM.createRoot`; ensure single `<script type="module">` in `index.html` points to `src/main.jsx`. _(PSC-2)_
- [x] 3.3 Update `src/App.jsx` to be a minimal functional component (e.g., renders a `<h1>Melodex</h1>`); import and apply at least one Tailwind class. _(PSC-2, BTL-3)_
- [x] 3.4 Create placeholder files: `src/engine/index.js`, `src/audio/index.js`, `src/store/index.js`, `src/components/index.js` — each with a one-line comment describing its future purpose. _(PSC-1)_
- [x] 3.5 Verify `src/lib/utils.js` exists with the `cn()` function (generated by shadcn or create manually: `import { clsx } from 'clsx'; import { twMerge } from 'tailwind-merge'; export function cn(...inputs) { return twMerge(clsx(inputs)); }`). _(BTL-4)_

---

## Phase 4: Testing Infrastructure

- [x] 4.1 Create `src/__tests__/setup.js` containing `import '@testing-library/jest-dom';`. _(TST-1)_
- [x] 4.2 Create `src/__tests__/smoke.test.js` with two tests: (a) renders `<App />` via RTL `render()` and asserts `container` is in the document; (b) asserts `document.body` is defined. _(TST-2)_
- [x] 4.3 Run `npm run test` and confirm both smoke tests pass and exit code is 0. _(TST-2, AC-3)_

---

## Phase 5: CI Pipeline

- [x] 5.1 Create `.github/workflows/ci.yml` with triggers on `push` (all branches) and `pull_request` (targeting `main`); job runs on `ubuntu-latest`, Node 20 with `cache: 'npm'`; steps: checkout → setup-node → `npm ci` → `npm run lint` → `npm run test` → `npm run build`. _(CI-1, CI-2)_
- [x] 5.2 Confirm `package-lock.json` exists and is committed (generated automatically by npm installs above). _(CI-2, AC-8)_

---

## Phase 6: Final Verification

- [x] 6.1 Run `npm run dev` — verify dev server starts at `http://localhost:5173` without errors. _(AC-1, BTL-1, PSC-2)_
- [x] 6.2 Run `npm run build` — verify exit code 0 and `dist/` contains `index.html`, `.js`, and `.css` files. _(AC-2, BTL-2)_
- [x] 6.3 Run `npm run lint` — verify exit code 0 with no errors on the scaffold. _(AC-4, LNT-1)_
- [x] 6.4 Run `npm run test` — verify all smoke tests pass. _(AC-3, TST-2)_
- [x] 6.5 Verify all required directories exist: `src/engine/`, `src/audio/`, `src/store/`, `src/components/`, `src/components/ui/`, `src/__tests__/` each with at least one file. _(AC-7, PSC-1)_
- [x] 6.6 Verify `src/lib/utils.js` exports `cn()` and `Button` is importable from `@/components/ui/button`. _(AC-6, BTL-4)_
